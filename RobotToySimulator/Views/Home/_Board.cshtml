@using RobotToySimulator.Models

@model RobotToySimulatorModel

<div class="row">
    
    <div class="col-md-12">
        <div>0,0</div>
        <table id="testTable">
            <tbody>
                @for (var r = 1; r <= Model.RowBoard; r++)
                {
                    <tr>
                        @for (var c = 1; c <= Model.ColumnBoard; c++)
                        {
                            <td>
                                @if (c == 1 && r == 5)
                                {
                                    <div id="fighter-jet">
                                        <i id="iconJet" class="fa fa-fighter-jet"></i>
                                    </div>
                                }
                            </td>
                        }
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
